# `device`

device.id
device.name

device.type.id
device.type.name

device.vendor
device.model
device.version.ver

device.shelf. {device}
device.card. {device}
device.port. {device}

device.system. {system}
device.system.cpu.arch.name
